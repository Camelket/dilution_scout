const config = {
	// connectionString: "postgres://postgres:postgres@localhost/users_db",
	// host: "localhost",
	// user: "postgres",
	// password: 'postgres',
	// port: 5432,
	// database: "users_db"
	// };

	connectionString: "postgres://postgres:admin@localhost/users_db",
	host: "localhost",
	user: "postgres",
	password: 'admin',
	port: 5432,
	database: "users_db"
	};

module.exports = config;